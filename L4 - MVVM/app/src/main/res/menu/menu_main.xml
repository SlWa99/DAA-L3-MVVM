<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_sort_by_creation"
        android:title="Trier par date de création"
        app:showAsAction="never" />

    <item android:id="@+id/action_sort_by_schedule"
        android:title="Trier par date prévue"
        app:showAsAction="never" />

    <item android:id="@+id/action_add_note"
        android:title="Créer une Note aléatoire"
        app:showAsAction="never" />

    <item android:id="@+id/action_delete_all"
        android:title="Supprimer toutes les Notes"
        app:showAsAction="never" />
</menu>